version: "3.1"
services:
    message_broker:
        image: rabbitmq:management
        environment:
            - RABBITMQ_DEFAULT_USER=user
            - RABBITMQ_DEFAULT_PASS=password
            # - RABBITMQ_DEFAULT_VHOST=0.0.0.0
        ports:
            - "8081:15672"
        restart: always
    configurator:
        image: nimashoghi/configurator-server
        environment:
            - SCHEMA_FILE=/app/settings.schema.json
            - SETTINGS_FILE=/app/settings.json
            - UPDATE_PASSWORD=wG6gIvi7jE49yaNH5gRbNMCiUvcTn5IOCClsIPhcwkysE1i63wM41hml8GdRTTT6
        volumes:
            - ./settings.json:/app/settings.json
            - ./settings.schema.json:/app/settings.schema.json
        ports:
            - "80:80"
    youtube_service:
        build: .
        command: python -m app.services.youtube
        environment:
            - SETTINGS_FILE=/app/settings.json
        volumes:
            - ./tmp/youtube-service:/tmp/youtube2podbean/
            - ./logs/youtube-service:/app/logs
            - ./pickles:/app/pickles
            - ./settings.json:/app/settings.json
        restart: always
        stdin_open: true
        tty: true
        depends_on:
            - message_broker
            - configurator
    podbean_service:
        build: .
        command: python -m app.services.podbean
        environment:
            - SETTINGS_FILE=/app/settings.json
        volumes:
            - ./tmp/podbean-service:/tmp/youtube2podbean/
            - ./logs/podbean-service:/app/logs
            - ./pickles:/app/pickles
            - ./settings.json:/app/settings.json
        ports:
            - "23808:23808"
        restart: always
        stdin_open: true
        tty: true
        depends_on:
            - message_broker
            - configurator
            - youtube_service
    discord_service:
        build: .
        command: python -m app.services.discord
        environment:
            - SETTINGS_FILE=/app/settings.json
        volumes:
            - ./tmp/discord-service:/tmp/youtube2podbean/
            - ./logs/discord-service:/app/logs
            - ./pickles:/app/pickles
            - ./settings.json:/app/settings.json
        restart: always
        stdin_open: true
        tty: true
        depends_on:
            - message_broker
            - configurator
            - youtube_service
    wordpress_service:
        build: .
        command: python -m app.services.wordpress
        environment:
            - SETTINGS_FILE=/app/settings.json
        volumes:
            - ./tmp/wordpress-service:/tmp/youtube2podbean/
            - ./logs/wordpress-service:/app/logs
            - ./pickles:/app/pickles
            - ./settings.json:/app/settings.json
        restart: always
        stdin_open: true
        tty: true
        depends_on:
            - message_broker
            - configurator
            - youtube_service
    wordpress:
        image: wordpress
        restart: always
        ports:
            - 8080:80
        environment:
            WORDPRESS_DB_HOST: db
            WORDPRESS_DB_USER: exampleuser
            WORDPRESS_DB_PASSWORD: examplepass
            WORDPRESS_DB_NAME: exampledb
        depends_on:
            - db
    db:
        image: mysql:5.7
        restart: always
        environment:
            MYSQL_DATABASE: exampledb
            MYSQL_USER: exampleuser
            MYSQL_PASSWORD: examplepass
            MYSQL_RANDOM_ROOT_PASSWORD: "1"
        volumes:
            - mysql-data-volume:/var/lib/mysql
volumes:
    mysql-data-volume:
